import { WillApplicationPipe } from './will-application.pipe';

describe('WillApplicationPipe', () => {
  it('create an instance', () => {
    const pipe = new WillApplicationPipe();
    expect(pipe).toBeTruthy();
  });
});
