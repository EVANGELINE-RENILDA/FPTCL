import { TrustWithdrawalPipePipe } from './trust-withdrawal-pipe.pipe';

describe('TrustWithdrawalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustWithdrawalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
