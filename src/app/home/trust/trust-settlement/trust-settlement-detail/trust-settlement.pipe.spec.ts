import { TrustSettlementPipe } from './trust-settlement.pipe';

describe('TrustSettlementPipe', () => {
  it('create an instance', () => {
    const pipe = new TrustSettlementPipe();
    expect(pipe).toBeTruthy();
  });
});
