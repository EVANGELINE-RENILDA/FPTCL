import { TrustApplicationPipePipe } from './trust-application-pipe.pipe';

describe('TrustApplicationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustApplicationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
