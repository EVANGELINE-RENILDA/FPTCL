import { ProfitandlossPipe } from './profitandloss.pipe';

describe('ProfitandlossPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfitandlossPipe();
    expect(pipe).toBeTruthy();
  });
});
