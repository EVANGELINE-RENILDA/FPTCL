import { BalancesheetmonthlyPipe } from './balancesheetmonthly.pipe';

describe('BalancesheetmonthlyPipe', () => {
  it('create an instance', () => {
    const pipe = new BalancesheetmonthlyPipe();
    expect(pipe).toBeTruthy();
  });
});
