import { TrailbalancePipe } from './trailbalance.pipe';

describe('TrailbalancePipe', () => {
  it('create an instance', () => {
    const pipe = new TrailbalancePipe();
    expect(pipe).toBeTruthy();
  });
});
