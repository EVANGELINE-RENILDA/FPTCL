import { ExpandAllDirective } from './expand-all.directive';

describe('ExpandAllDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandAllDirective();
    expect(directive).toBeTruthy();
  });
});
